INCLUDE(TribitsAddExecutableAndTest)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

SET(SOURCES
  main.cpp
  )
  
TRIBITS_ADD_EXECUTABLE_AND_TEST(
  PanzerPoissonExample 
  SOURCES ${SOURCES}
  COMM serial mpi
  NUM_MPI_PROCS 4
  )
